export interface IProject {
  title: string;
  url: string;
  imageUrl: string;
  altText: string;
  description: string;
}

export interface ISkill {
  skill: string;
  level: number;
  imageUrl: string;
  altText: string;
}

export interface IColor {
  color: string;
}
